# name: SonarQube Analysis

# # ✅ Add this block
# permissions:
#   contents: read
#   issues: write
#   pull-requests: write
#   checks: write

# on:
#   pull_request:
#     types: [opened, synchronize, reopened]
#   push:
#     branches:
#       - main
#       - develop
#       - master

# jobs:
#   sonarcloud:
#     name: SonarCloud Scan
#     runs-on: ubuntu-latest

#     steps:
#       - name: Checkout repository
#         uses: actions/checkout@v4
#         with:
#           fetch-depth: 0  # Shallow clones disabled for better analysis

#       - name: Cache SonarCloud packages
#         uses: actions/cache@v3
#         with:
#           path: ~/.sonar/cache
#           key: ${{ runner.os }}-sonar
#           restore-keys: ${{ runner.os }}-sonar

#       - name: SonarCloud Scan
#         uses: SonarSource/sonarqube-scan-action@v6
#         env:
#           GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
#           SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
#         with:
#           args: >
#             -Dsonar.projectKey=${{ secrets.SONAR_PROJECT_KEY }}
#             -Dsonar.organization=${{ secrets.SONAR_ORGANIZATION }}
#             -Dsonar.pullrequest.key=${{ github.event.pull_request.number }}
#             -Dsonar.pullrequest.branch=${{ github.head_ref }}
#             -Dsonar.pullrequest.base=${{ github.base_ref }}

#       - name: Check Quality Gate
#         id: sonarqube-quality-gate-check
#         uses: sonarsource/sonarqube-quality-gate-action@master
#         timeout-minutes: 5
#         env:
#           SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
#         continue-on-error: true

#       - name: Post Quality Gate Status
#         if: always() && github.event_name == 'pull_request'
#         uses: actions/github-script@v7
#         with:
#           # ✅ Add this line so github-script uses the token
#           github-token: ${{ secrets.GITHUB_TOKEN }}
#           script: |
#             const qualityGate = '${{ steps.sonarqube-quality-gate-check.outputs.quality-gate-status }}';
#             const emoji = qualityGate === 'PASSED' ? '✅' : '❌';

#             github.rest.issues.createComment({
#               issue_number: context.issue.number,
#               owner: context.repo.owner,
#               repo: context.repo.repo,
#               body: `${emoji} **SonarCloud Quality Gate**: ${qualityGate}\n\nView detailed report: ${{ steps.sonarqube-quality-gate-check.outputs.project-status-url }}`
#             });



name: SonarQube Analysis

permissions:
  contents: read
  pull-requests: write
  checks: write
  issues: write

on:
  pull_request:
    types: [opened, synchronize, reopened]
  push:
    branches:
      - main
      - develop
      - master

jobs:
  sonarcloud:
    name: SonarCloud Scan
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r scripts/requirements.txt
          pip install pytest pytest-cov

      - name: Run tests with coverage
        run: |
          pytest --cov=scripts --cov-report=xml

      - name: Cache SonarCloud packages
        uses: actions/cache@v3
        with:
          path: ~/.sonar/cache
          key: ${{ runner.os }}-sonar
          restore-keys: ${{ runner.os }}-sonar

      # -----------------------------
      # PR ANALYSIS (Auto-detected)
      # -----------------------------
      - name: SonarCloud Scan (Pull Request)
        if: github.event_name == 'pull_request'
        uses: SonarSource/sonarqube-scan-action@v6
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        with:
          args: >
            -Dsonar.projectKey=${{ secrets.SONAR_PROJECT_KEY }}
            -Dsonar.organization=${{ secrets.SONAR_ORGANIZATION }}

      # -----------------------------
      # BRANCH ANALYSIS (Push builds)
      # -----------------------------
      - name: SonarCloud Scan (Branch)
        if: github.event_name == 'push'
        uses: SonarSource/sonarqube-scan-action@v6
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        with:
          args: >
            -Dsonar.projectKey=${{ secrets.SONAR_PROJECT_KEY }}
            -Dsonar.organization=${{ secrets.SONAR_ORGANIZATION }}
            -Dsonar.branch.name=${{ github.ref_name }}

      # -----------------------------
      # QUALITY GATE CHECK
      # -----------------------------
      - name: Check Quality Gate
        id: quality_gate
        if: always()
        uses: sonarsource/sonarqube-quality-gate-action@v1
        timeout-minutes: 5
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        continue-on-error: true

      # -----------------------------
      # COMMENT ON PR
      # -----------------------------
      # - name: Post Quality Gate Status
      #   if: always() && github.event_name == 'pull_request'
      #   uses: actions/github-script@v7
      #   with:
      #     github-token: ${{ secrets.GITHUB_TOKEN }}
      #     script: |
      #       const status = '${{ steps.quality_gate.outputs.quality-gate-status }}';
      #       const url = '${{ steps.quality_gate.outputs.project-status-url }}';
      #       const emoji = status === 'PASSED' ? '✅' : '❌';

      #       github.rest.issues.createComment({
      #         issue_number: context.issue.number,
      #         owner: context.repo.owner,
      #         repo: context.repo.repo,
      #         body: `${emoji} **SonarCloud Quality Gate:** ${status}\n\n🔎 ${url}`
      #       });

